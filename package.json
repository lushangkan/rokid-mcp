{
  "name": "rokid-mcp",
  "private": true,
  "packageManager": "bun@1.2.5",
  "workspaces": [
    "apps/relay-server",
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "dev:relay": "bun run --cwd apps/relay-server dev"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
